import ScopedCSSCache from './css/ScopedCSSCache';
/**
 * Render settings for shadow root.
 */
export default class ShadowRootRenderOptions {
    openShadowRoots: boolean;
    appendScopedCSSToHead: boolean;
    cssCache: ScopedCSSCache;
}
